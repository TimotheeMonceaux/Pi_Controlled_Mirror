# If not run externally, we need to launch the server (on port 3000)
#npm start

# To run the front, use the following command
chromium-browser --app=http://localhost:3000 --start-fullscreen

# Terminate
#pkill chromium